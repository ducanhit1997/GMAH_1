
@{
    ViewBag.Title = "Thiết lập thành phần điểm";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@Styles.Render("~/css/datatable")

<div class="card card-secondary">
    <div class="card-header">
        <div class="row">
            <div class="col-4">
                <div class="row">
                    <label for="selectedSemester" class="col-sm-2 col-form-label">Năm học</label>
                    <div class="col-sm">
                        <div class="form-group">
                            <select class="form-control" name="IdYear" id="IdYear" onchange="LoadStudyFieldByYeadId()">
                            </select>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-4">
                <div class="row">
                    <label for="selectedClass" class="col-sm-2 col-form-label">Bang</label>
                    <div class="col-sm">
                        <div class="form-group">
                            <select class="form-control" name="IdStudyField" id="IdStudyField" onchange="LoadScoreType()">
                            </select>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-4">
                <div class="row">
                    <label for="selectedClass" class="col-sm-2 col-form-label">Môn</label>
                    <div class="col-sm">
                        <div class="form-group">
                            <select class="form-control" name="IdSubject" id="IdSubject" onchange="LoadScoreType()">
                            </select>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- /.card-header -->

    <div class="card-body">
        <div>
            <div class="text-right" style="padding-bottom: 10px">
                <button class="btn btn-primary"
                        onclick="CreateNew()">
                    <i class="fas fa-plus"></i>
                    Tạo thành phần điểm
                </button>
                <button class="btn btn-success"
                        onclick="OpenModalImportListScore()">
                    <i class="fas fa-plus"></i>
                    Import từ file
                </button>
            </div>
        </div>

        <table id="result" class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>Tên thành phần điểm</th>
                    <th>Hệ số</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>

<script>
    var _ID_SEMESTER = @ViewBag.IdSemester;
</script>

@Html.Partial("_ModalScoreType")
@Html.Partial("_ModalUploadFileListScore")

@section scripts {
    @Scripts.Render("~/js/datatable")
    <script src="~/Assests/Admin/js/pages/ListScoreType.js"></script>
    <script src="~/Assests/Admin/js/pages/InfoScoreType.js"></script>
    <script src="~/Assests/Admin/js/pages/ImportListScoreExcel.js"></script>
}
